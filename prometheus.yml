global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "ultramac-mcp"
    static_configs:
      - targets: ["localhost:3010"]
    metrics_path: "/metrics"
    scrape_interval: 10s
